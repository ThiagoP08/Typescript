export interface Filme{
    id: string,
    titulo: string,
    img: string,
    genero: number;
}

export interface Categoria{
    id: string,
    titulo: string,
    img: string,
    genero: number;
}